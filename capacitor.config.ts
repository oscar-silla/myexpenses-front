import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mypersonalbook.app',
  appName: 'my-personal-book',
  webDir: 'dist/my-personal-book/browser',
};

export default config;
